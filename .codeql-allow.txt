# Findings we have looked at and accepted, so `make codeql` stays actionable.
#
# One per line: <rule-id> <path> # why
#
# A path matches by prefix, so a directory covers everything under it. Nothing
# belongs here that has not actually been read -- an entry is a claim that a
# human checked it, and it should carry the reason that check reached.

py/unused-global-variable lemma-backend/app/modules/workspace/services/sandbox_composition.py  # Read on a later call at line 46 via `global`; the query does not follow reads across calls.
py/partial-ssrf lemma-backend/app/modules/workspace/api/controllers/port_proxy_controller.py  # The host comes only from the provider's base URL; the caller controls the path component alone, set via copy_with(path=...).
py/call-to-non-callable lemma-backend/app/modules/function/events/handlers.py  # The site is annotated `uow_factory: UnitOfWorkFactory`, a callable protocol. CodeQL inferred the type from one of four unrelated test-local classes named `_UowFactory`, three of which have no `__call__`.
py/non-iterable-in-for-loop lemma-backend/scripts/export_agent_tool_schemas.py  # `for member in AgentToolset:` iterates an enum *class* via `EnumMeta.__iter__`, which CodeQL reads as iterating a bare `type`. Checked by running it: 12 members.
py/ineffectual-statement lemma-backend/app/modules/pod/domain/ports.py  # `...` is the body of a `Protocol` method, which is what PEP 544 and typeshed prescribe — not a statement that was meant to do something. Every hit in this file is one of those stubs; the query does not model Protocol bodies. It only surfaced when a diff touched neighbouring lines.
py/ineffectual-statement lemma-backend/app/modules/agent/domain/ports.py  # The same Protocol stubs as the pod ports above, and the same query. Read all seven the bot reported: every one is the `...` closing an `async def` in `ConversationRepository` or `AgentRepository`. This change declares twenty-four more of them, which is why they surfaced together.
py/ineffectual-statement lemma-backend/app/core/authorization/grants.py  # `...` again, under `@property`: `ResourceGrantInputProtocol` declares its three members read-only so a frozen dataclass satisfies it, and a property stub needs a body. Those three lines are the only hits in the file; anything else this query finds here would be worth reading rather than accepting.
py/ineffectual-statement lemma-backend/app/modules/connectors/domain/ports.py  # The same Protocol stubs as the pod and agent ports above. Both hits are the `...` closing an `async def` — `promote_next_default` on `AccountRepositoryPort` and `connect_with_credentials` on `AuthProviderPort`. There are 52 identical stubs in this file already; these two surfaced only because the diff added them.
py/ineffectual-statement lemma-backend/app/modules/datastore/domain/ports.py  # The same Protocol stubs as the pod, agent and connectors ports above. The single hit is the `...` closing `open_download` on `DatastoreStoragePort`. There are 68 identical stubs in this file already, none of them flagged; this one surfaced only because the diff added it.
py/ineffectual-statement lemma-backend/app/modules/identity/domain/ports.py  # The same Protocol stubs as the pod, agent, connectors and datastore ports above. Both hits are the `...` closing an `async def` -- `existing_ids` on `UserRepositoryPort` and `get_many` on `OrganizationRepositoryPort`. Read both: there are 40-odd identical stubs in this file already, none of them flagged, and these two surfaced only because the diff added them.
py/ineffectual-statement lemma-backend/app/modules/identity/contracts/__init__.py  # The same shape one file over: the single hit is the `...` closing `existing_ids` on the `UserReader` protocol, which is how the datastore asks identity which of a set of ids name real users. The `get` stub directly above it is identical and unflagged, being unchanged by the diff.
py/ineffectual-statement lemma-backend/app/modules/connectors/domain/results.py  # The same Protocol stubs again, this time under `@property`: `_HttpResponseLike` declares `headers` and `content` read-only so an `httpx.Response` satisfies it covariantly, and a property stub needs a body. Those two lines are the only hits in the file. Declaring them as plain attributes instead would make the protocol invariant and stop matching `httpx.Headers` at all.
py/ineffectual-statement lemma-backend/app/modules/connectors/tests/e2e/test_mcp_connector_auth_e2e.py  # `await task` after `task.cancel()`, inside `suppress(CancelledError)` — the canonical way to let a cancelled task finish unwinding. The await's effect is the suspension, which is the whole point; only its value is discarded, and that is what the query sees.
py/unreachable-statement lemma-backend/app/modules/connectors/tests/unit/test_discovery_failure_translation.py  # Every hit is a line after a `raise` that sits inside `with pytest.raises(...)`, which catches it — so control continues and the assertions run. The query does not model `pytest.raises`. Checked by running the file: 10 passed, and the assertions the query calls unreachable are the ones doing the checking.
py/unreachable-statement lemma-backend/app/modules/connectors/tests/unit/test_failure_classification.py  # The same `pytest.raises` shape: `return caught.value` after the `with` block. Checked by executing that exact function outside pytest — it returns an `OperationExecutionInfrastructureError`, so the line plainly runs.
py/mixed-returns lemma-backend/app/modules/connectors/tests/e2e/test_mcp_third_party_server_e2e.py  # `_discover` returns explicitly in the `try` and ends its `except*` handler with `pytest.skip(...)`, which raises `Skipped` — so there is no implicit `None` return to mix with. Checked by calling `pytest.skip` and reading the exception type.
py/ineffectual-statement lemma-backend/app/modules/connectors/services/install_service_seam.py  # The fifth file of Protocol stubs, and the same `...` bodies as the four above. Read all seven the query reports: every one closes an `async def` or a `def` on `InstallServiceSeam`, which exists only to declare what the install helpers use of the service. Nothing else in the file is a bare expression.
py/non-iterable-in-for-loop lemma-backend/app/modules/datastore/tests/unit/test_datastore_api_helpers.py  # `for operator in RecordFilterOperator:` iterates an enum *class* via `EnumMeta.__iter__`, the same shape as the export_agent_tool_schemas.py entry above. The test asserts every operator is named in RECORD_FILTER_DESCRIPTION; it passes, so the loop plainly runs.
js/superfluous-trailing-arguments lemma-typescript/src/__tests__/queue-a-steer-for-an-acp-turn.test.ts  # `new StorageEvent("storage", { key })` is the constructor the HTML spec defines; the second argument is the `StorageEventInit` dictionary. CodeQL's bundled Closure extern for `webstorage.js` still declares the pre-`EventInit` signature, so every standards-correct call looks superfluous to it — the same shape the query's own changelog records fixing for `TransformStream` in 2.4.4. Checked rather than assumed: dispatching that event in jsdom gives `event.key === "lemma.queued-steers.c1"`, and the test only passes because the listener compares that key.
py/ineffectual-statement lemma-backend/app/modules/identity/services/email_challenges.py  # The same Protocol stubs as the ports files above: both hits are the `...` closing `__call__` on `ChallengeEmailSender` and `ChallengeSendLimits`, the two callables the challenge service is constructed with. Nothing else in the file is a bare expression.
py/unused-import lemma-backend/app/modules/agent_surfaces/infrastructure/models.py  # The four onboarding models are re-exported so Alembic's metadata sees their tables through the module it already imports; nothing reads the names here, which is the point. `# noqa: F401` says the same thing to ruff on the line itself.
py/ineffectual-statement lemma-backend/app/modules/agent_surfaces/services/personal_dm_routes.py  # The same Protocol stub as the ports files above: the single hit is the `...` closing `_get_or_create_conversation_link` on `ConversationLinker`, which names the one thing context-building needs from the ingress service so this module does not have to import its own module's FastAPI wiring to get it.
py/ineffectual-statement lemma-backend/app/modules/web_login/api/controllers/web_login_controller.py  # The same Protocol stubs as the pod, agent, connectors, datastore and identity ports above. Both hits are the `...` closing an `async def` on `BrowserView`, the two calls the web-login routes make against the browser -- declared as a protocol so the routes can take it as a dependency rather than have their tests patch a module-level factory. A docstring-only body was tried first and basedpyright rejects it: "must return value on all code paths".
# ── Onboarding's module-graph cycles ─────────────────────────────────────────
#
# Seven hits, all of one cycle. Traced it rather than assumed: every return path
# closes through `agent_surfaces.contracts.email_surfaces ->
# agent_surfaces.api.dependencies -> agent_surfaces.services.ingress_service`,
# which predates this work, and the two imports that put onboarding on it are
# made inside the function that needs them -- `ingress_service.py:332` and
# `identity_resolution_service.py:181` -- which is the remedy for a cycle, not
# an instance of one. Nothing imports in a cycle at run time; `app.events`
# imports clean and the worker boots. `check_architecture.py`, which is this
# repository's gate for exactly this question, reports 0 cycles. Removing the
# edges CodeQL names means moving `validate_personal_dm_route` and the
# contracts it reads out from under the services layer, which is a change to
# how contracts are arranged and belongs to one.
py/cyclic-import lemma-backend/app/modules/agent_surfaces/services/identity_resolution_service.py  # See the note above: the closing import is the function-level one at line 181.
py/cyclic-import lemma-backend/app/modules/agent_surfaces/services/ingress_service.py  # See the note above: the closing import is the function-level one at line 332.
py/cyclic-import lemma-backend/app/modules/agent_surfaces/services/personal_dm_routes.py  # See the note above. Both hits are module-level reads of `agent.contracts.provisioning` and `identity.contracts.onboarding`; the cycle closes through the deferred import in `ingress_service`.
py/cyclic-import lemma-backend/app/modules/agent_surfaces/services/verified_surface_identity.py  # See the note above; same shape, closing through `ingress_service -> identity_resolution_service`.
py/cyclic-import lemma-backend/app/modules/identity/contracts/onboarding.py  # See the note above; the path back runs first_workspace -> pod.contracts.personal_workspace -> pod.api.dependencies -> pod_service -> agent_surfaces.contracts.email_surfaces.
py/cyclic-import lemma-backend/app/modules/identity/services/first_workspace.py  # See the note above; same path as identity.contracts.onboarding.
py/cyclic-import lemma-backend/app/modules/pod/contracts/personal_workspace.py  # See the note above; the path back runs agent.contracts.provisioning -> agent.api.dependencies -> agent_service -> agent_surfaces.contracts.email_surfaces.
py/regex-injection lemma-backend/app/modules/workspace/services/ws_bridge.py  # The pattern is `cors_origin_regex`, deployment configuration, and the *exact same string* is already handed to Starlette's `CORSMiddleware(allow_origin_regex=...)` at app.py:402 and auth_app.py:25, where it is compiled and run against every HTTP request. The tainted value CodeQL follows is the operator's own config, not anything a caller sends; the caller controls only the subject string. Refusing it here while accepting it on every HTTP request would be inconsistent rather than safer. Checked the call: `re.fullmatch` is wrapped in `except re.error`, so a pattern that will not compile refuses instead of raising.
py/clear-text-logging-sensitive-data lemma-backend/scripts/whatsapp_numbers.py  # Read every one of the seven hits: the script prints `phone_number_id`, `display_phone_number`, `waba_id`, `role`, `status` and `notes`, and never a credential. The taint reaches `print` through the `declared` generator at :88, which iterates `(name, value)` pairs and yields `name` alone where `value` is truthy — so the secret is *tested* in the expression and the label is what leaves it. That is the deliberate design: an operator needs to see which numbers carry their own token without the tool ever putting one on a terminal or in shell history. The three secrets are taken only from named environment variables and are never echoed back.
py/ineffectual-statement lemma-backend/app/modules/agent_surfaces/services/credential_resolver.py  # The same Protocol stub as the ports files above: the single hit is the `...` closing `get_by_phone_number_id` on `PooledNumberReader`, which names the one thing pooled-credential resolution needs of the number repository so a test can answer it without Postgres. Nothing else in the file is a bare expression.
py/clear-text-logging-sensitive-data lemma-backend/scripts/whatsapp_numbers.py  # The two expressions are `phone_number_id` and `display_phone_number` in the `list` subcommand's output, which CodeQL reads as private data because they are phone numbers. They are, and printing them is the command: this is an operator running a CLI against their own deployment to see which numbers the pool holds, and a listing that redacts the numbers lists nothing. `print` to stdout, not a logger, so none of it reaches log aggregation unless the operator sends it there. The tokens this file also handles never reach the line -- each one becomes a bare name under an `if`, which is the fix two commits above this entry.
