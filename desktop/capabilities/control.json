{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "control-capability",
  "description": "Bundled local Control Center with narrowly checked application commands",
  "windows": ["control"],
  "permissions": ["core:default"]
}
