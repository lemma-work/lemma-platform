{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main-capability",
  "description": "The bundled splash/installer page drives setup over IPC. Scoped to the main webview rather than the main window: Local settings is a child webview of that same window, and capability matching is window OR webview, so a window scope here would hand the splash commands to Local settings too.",
  "webviews": ["main"],
  "permissions": [
    "core:default",
    "allow-start",
    "allow-stop",
    "allow-restart",
    "allow-open-app",
    "allow-open-logs",
    "allow-installer-log",
    "allow-diagnostic-logs",
    "allow-choose-connection-mode",
    "allow-set-connection-mode",
    "allow-get-state",
    "allow-login",
    "allow-prepare-runtime"
  ]
}
