[project]
name = "lemma-desktop-mlx-runtime"
version = "0.1.0"
requires-python = ">=3.14,<3.15"
dependencies = [
    "mlx-lm==0.31.3",
]

[tool.uv]
package = false
required-environments = [
    "sys_platform == 'darwin' and platform_machine == 'arm64'",
]
