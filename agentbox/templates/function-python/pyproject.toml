[project]
name = "agentbox-function-environment"
version = "1.0.0"
requires-python = ">=3.14,<3.15"
dependencies = [
    "httpx>=0.28.1",
    "lemma-sdk",
    "pydantic>=2.11.0",
]

[tool.uv]
package = false

[tool.uv.sources]
lemma-sdk = { path = "../../../lemma-python" }
