node_modules/
dist/
build/
.venv/
.pytest_cache/
.ruff_cache/
*.egg-info/
.DS_Store
.env
.env.*
!.env.example

# Coverage reports
.coverage
.coverage.*
coverage.xml
coverage-*.xml
htmlcov/
coverage/
lcov.info
*.lcov

# Python bytecode
__pycache__/
*.py[cod]

# Dev stack pid files (written by `make dev` / `make stop`)
/.dev-pids
*.dev-backend.pid
*.dev-frontend.pid
*.dev-infra.pid

# Local-disk object/file storage used in dev when no real S3/GCS is configured
# (see local_object_storage_root/local_file_storage_root in app/core/config.py)
/.local/

# Claude Code local tooling (launch configs, worktrees) — not part of the repo
/.claude/
