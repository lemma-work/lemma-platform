node_modules/
dist/
build/
.venv/
.pytest_cache/
.ruff_cache/
*.egg-info/
.DS_Store
.env
.env.*
!.env.example

# Local runtime data (object storage, generated files, model caches)
/.local/
/.benchmark-results/

# Coverage reports
.coverage
.coverage.*
coverage.xml
coverage-*.xml
htmlcov/
coverage/
coverage-agent-surfaces/
coverage-backend/
lemma-backend/coverage-agent-surfaces/
lemma-backend/coverage-backend/
lcov.info
*.lcov

# Python bytecode
__pycache__/
*.py[cod]

# Dev stack pid files (written by `make dev` / `make stop`)
/.dev-pids
*.dev-backend.pid
*.dev-frontend.pid
*.dev-infra.pid
.dev-cloudflared-*.pid
.dev-logs/
# Rust build outputs. The native stack is one cargo workspace under desktop/,
# so **/target/ now covers a single desktop/target/ rather than six.
**/target/
**/.build/

# Packaged Desktop runtime archives are reproducible release artifacts, and a
# bundled build stages both the payloads and the manifest that names them as
# app resources. None of that belongs in git — and the tracked
# desktop/runtime/lemma-local.json must stay the online-shaped placeholder, not
# whatever the last local `make desktop-dmg` produced.
/desktop/runtime/bundled/
/desktop/runtime/download/

# Local scratch output from screenshot/PDF tooling
/out/
/output/
/tmp/
.claude/launch.json

# CodeQL databases and SARIF output from `make codeql`.
.codeql/

# Local benchmark corpus (~310MB of arXiv PDFs) used by the datastore
# load tests. Rebuild with:
#   uv run python lemma-backend/load_tests/fetch_arxiv_corpus.py
# The script is the source of truth for which papers are in it, so the
# bytes stay local and never go to GitHub.
benchmark-corpus/
benchmark-corpus
