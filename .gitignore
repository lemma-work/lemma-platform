node_modules/
dist/
build/
.venv/
.pytest_cache/
.ruff_cache/
*.egg-info/
.DS_Store
.env
.env.*
!.env.example

# Local runtime data (object storage, generated files, model caches)
/.local/
/.benchmark-results/

# Coverage reports
.coverage
.coverage.*
coverage.xml
coverage-*.xml
htmlcov/
coverage/
coverage-agent-surfaces/
coverage-backend/
lemma-backend/coverage-agent-surfaces/
lemma-backend/coverage-backend/
lcov.info
*.lcov

# Python bytecode
__pycache__/
*.py[cod]

# Dev stack pid files (written by `make dev` / `make stop`)
/.dev-pids
*.dev-backend.pid
*.dev-frontend.pid
*.dev-infra.pid
.dev-cloudflared-*.pid
.dev-logs/
# Rust build outputs (desktop and local daemon)
**/target/
**/.build/
/local-runtime/guestd/target-linux/

# Packaged Desktop runtime archives are reproducible release artifacts. Keep
# their signed manifests and build inputs in git, but never commit the large
# generated ZIP payloads.
/desktop/runtime/bundled/*.zip
