import { PodIndexCardsSkeleton } from '@/components/pod/route-skeletons';

/** Workflows settle into a card grid. */
export default function WorkflowsLoading() {
    return <PodIndexCardsSkeleton tabs={5} />;
}
