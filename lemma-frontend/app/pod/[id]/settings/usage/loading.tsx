import { PodSettingsSkeleton } from '@/components/pod/route-skeletons';

/** Usage settles into panels in a column. */
export default function PodUsageLoading() {
    return <PodSettingsSkeleton rows={4} />;
}
