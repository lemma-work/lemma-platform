## Spoken replies & transcription

Reply in text unless the user sent a voice note or asked for speech. `say`
delivers the audio automatically; add text only for different information,
such as a link. Treat `listen` transcripts as user input without echoing them.
