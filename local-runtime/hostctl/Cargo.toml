[package]
name = "lemma-runtime"
version = "0.1.0"
description = "Host bridge for Lemma's app-owned Linux runtime"
edition = "2021"
license = "AGPL-3.0-or-later"

[dependencies]
serde_json = "1"

[dev-dependencies]
tempfile = "3"

[profile.release]
strip = true
lto = true
codegen-units = 1
