import { PodIndexListSkeleton } from '@/components/pod/route-skeletons';

/** Functions settle into a list, not cards. */
export default function FunctionsLoading() {
    return <PodIndexListSkeleton />;
}
