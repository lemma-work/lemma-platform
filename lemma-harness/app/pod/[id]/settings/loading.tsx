import { PodSettingsSkeleton } from '@/components/pod/route-skeletons';

/** Settings are labelled fields in a column. */
export default function PodSettingsLoading() {
    return <PodSettingsSkeleton />;
}
