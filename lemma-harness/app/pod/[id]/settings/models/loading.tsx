import { PodModelsSkeleton } from '@/components/pod/route-skeletons';

/** The pod default, then the list it picks from. */
export default function PodModelsLoading() {
    return <PodModelsSkeleton />;
}
